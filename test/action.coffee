describe 'action', ->





    # fs.readFileAsync(process.cwd + '/.git/config')
    # .then(function(){
    #   return willExec(action)
    # })
    # .catch(isNotFound, function() {
    #   return willSkip(action).reason('NO_SIGNIFIER', 'No .git folder found, assuming not a git project.')
    # })
